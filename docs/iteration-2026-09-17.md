# 2026-09-17 反馈迭代

## 当前交付与反馈

真实泳池素材已在 Windows 完成重剪，并经授权上传到既有 video-2022。第二版取消黑底、预告、分类小字和叠化，同类动作使用连续固定名称，正常速度、静音。用户尚未确认第二版观感满意；不把可播放写成已验收。

本次把临时实现变成项目配置：plain 字幕、compact 编码、同名字幕合并、简洁方案模板；同步场景提示词、通用交付提示词和 AGENTS.md。明确反馈自动进入项目；冲突时最新意见优先，但不扩展为用户所有项目的永久偏好。

新增本地交付适配器，复用 video-cli 登录与 OSS API。回执支持失败重试，创建响应丢失时停止自动创建，防止重复记录；检查 READY、完整 HLS 和首尾分片解码。它不是后台队列或手机产品。

## 验证证据与边界

- 实际运行简洁合成 demo、MP4 全片解码，并查看生成的字幕预览。
- 新 check-delivery 命令实际读取已交付样片：READY，两档 HLS，各 65 分片、128.033 秒；首尾分片全部解码通过。未做手机真机操作验收。
- 上传失败、创建响应丢失、重复上传防护、处理超时、播放列表缺失、分片损坏等通过离线替身测试；新增 deliver 命令没有再次执行真实上传。此前真实上传成功不能冒充此新命令的端到端证据。
- 本次 Windows 实跑 pytest：45 passed、1 skipped；跳过项仅为系统缺少符号链接权限。ruff 和 doctor 通过。Linux CI 结果见 PR checks。
- 可选上传依赖新增 truststore，复用现有 CLI 的 requests/oss2；没有新增大型模型或付费 API。

## 昨日合并的范围

[PR #1](https://github.com/makewheels/video-editing/pull/1) 于 2026-09-16 合并，交付的是可运行引擎验证工作台。完整产品角度仍未完成：手机入口、服务器任务队列、订阅执行器和部署均不在已完成范围。GitHub 显示 makewheels 账号合并，无 PR review 记录；这些信息不能区分用户本人还是代理操作。

本轮修正完成边界，推送并提交 PR，不自动合并。未来手机全自助按用户要求暂缓；当前以剪好、反馈迭代、交付可播放链接为目标。私人素材、播放 ID、回执和凭证不进入 Git。
