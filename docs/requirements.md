# 讨论记录与需求基线

> 2026-09-18 晚最新纠正：先梳理教学项目；序号按项目而非片段，合并重复深水探底，序号与名称同一行；底板待定。按视频类型、剪辑风格、专业术语三层整理提示词。今天只记录，下一次按[接续清单](next-session-content-review.md)执行；此前v6规则与本条冲突时以本条为准。

> 2026-09-18 后续反馈：每个片段增加入场、出场动画和顺序编号，风格更活泼；保留本片教练原声、无配乐。重点是把经验固化为提示词，使后续只需简短反馈即可自主剪辑。见 [自主剪辑流程](autonomous-editing.md)。


> 2026-09-18 最新反馈：本片用于10节游泳课的学习成果展示和抖音推广。放大动作名称并加底板、短转场、对照参考片核对教学术语、后续最新反馈为不要配乐、恢复清楚的教练原声；替代本日较早的配乐混音要求及下文 09-17 的透明底/硬切/静音设置。实际进度见 [本次迭代](./iteration-2026-09-18.md)。旧例子仅保留回归用途。

> 2026-09-17 更新：最新反馈优先于下文历史基线：同类动作连续展示，一个动作一个固定名称；无预告、无黑底、不频繁换字幕；蹬壁归出发技巧。当前只做本地剪辑、反馈迭代和授权后 video-2022 手机链接交付，手机全自助暂缓。明确反馈直接更新项目提示词和必要实现。本次样片静音，不扩展为所有项目永久静音。

记录日期：2026-09-16。

## 用户目标

用户已有 AI 剪辑的游泳视频，认为剪辑速度快、大方向正确、覆盖动作的能力已经有价值，希望将经验沉淀成平台。主要使用入口是手机，计算和任务运行必须在服务器完成，不依赖 Mac。

## 五项需求

1. 上传多段素材，并组织为同一个剪辑工程。
2. 按自然语言要求和规则自动剪辑，生成成片。
3. 持续积累规则、个人偏好和反馈，供后续工程复用。
4. 手机上预览、下载、保存成片，用于用户手动发布抖音。
5. 第二期通过对话修改已有工程，生成新版并可回退。

自动发布抖音尚不在范围内。多用户商业服务、复杂手动时间线编辑器尚未确认为需求。

## 对现有成片的反馈

用户肯定：剪辑很快，大方向符合初衷，体现不同泳姿，证明 AI 能完成工作。

用户指出：

- 蹬壁出发不是泳姿，不应作为泳姿单独并列。
- 姿势之间切换过硬，希望字幕先行、整体节奏慢一点。
- 现场讲话与实际展示动作可能不在同一时间点。
- 倾向去掉原声，改用后期配音或音乐。

待样片验证的规则建议：保留完整动作周期；镜头停留和段落衔接先调整，再决定是否慢放；区分下一段预告与当前动作标签，避免字幕提前导致错误标注。

尚未决定：泳姿展示还是更广泛的游泳技能合集；默认音乐还是旁白；字幕提前量、各段时长、转场方式。不得将推荐值当成已确认要求。

## 三个阶段

| 阶段 | 目标 | 验收 |
| --- | --- | --- |
| 准备 | 验证已有点播链路、现有订阅执行路线、开源候选 | 手机上传和成片保存可用；服务器完成一次真实剪辑 |
| 第一期 | 上传素材到自动成片 | 手机上提交要求后可查看进度、预览、下载符合确认规则的成片 |
| 第二期 | 对话修改 | 按指定反馈修改对应片段、字幕或音轨，保留工程版本并可回退 |

## 讨论中的重要修正

- 拒绝将剪辑任务和工程记录放进 video-2022：点播与剪辑是独立平台。
- 拒绝依赖 Mac 运行：服务器处理，手机使用。
- 不采用默认需要 模型 API 凭证 的设计：用户只有 现有编程助手订阅。
- 优先开源复用，经真实素材验证后选择，不先重写完整平台。
- GPU 不是已确定依赖；用户允许需要时使用阿里云函数计算 GPU，以实际效果为目标。
- Multica 不是第一期已确定依赖。

## 本次完善的实施基线

通用方案和开源引擎验证层已实现，见 README。手机平台和服务器执行仍待实现。素材参考角色、内容覆盖、视觉证据、转场和时间冲突、声音模式、版本与规则快照必须可检查；不能只保存一段自然语言提示词。

合成演示的 0.4 秒转场、0.3 秒预告和 4.4 秒长度仅用于测试，不是用户已确认的默认剪辑风格。
