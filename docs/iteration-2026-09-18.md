# 2026-09-18 大字幕、教学名称与声音迭代

> 最新反馈：用户要求不要配乐，恢复清楚的教练原声。v5使用原素材同期音轨；下文v4混音为已被替代的历史版本。

本次用于10节游泳课的学习成果展示和抖音推广。用户要求放大字幕并加底板、自然转场、参考视频核对教学名称、音乐与低音量原声，并授权专用OSS bucket、RAM用户及Infisical存储凭据。

09-18反馈替代09-17的透明底、硬切和静音；继续保留无预告、同类固定名称、不覆盖原素材与旧成片。不按剪辑区间数宣传动作数量。

## 已实现

- `badge`：画幅宽度7.5%的白字、半透明深蓝圆角底板，无分类小字；旧`plain/card`方案保持原行为。
- `mixed`：背景音乐与原声分别设置音量，校验音乐来源、长度及源音轨；原有替换音乐模式不变。
- 同名字幕跨叠化持续显示，不在转场中闪灭。相同原素材连续展示仍保持连续镜头。
- 当前样片保留10条原素材，14个剪辑区间，124.93秒，720×1280、30fps。对照参考片分出抬头蛙泳与仰泳出发；参考片未作为镜头使用。
- 外部音乐使用Carefree / Kevin MacLeod / CC BY 4.0，截取、淡入淡出和调音；署名随交付并写入播放页说明，发布到抖音时保留。
- 独立私有OSS与专用RAM用户已建立；用户仅有该bucket的读写/分片权限。密钥唯一来源为Infisical 项目专用凭据目录，未写入Git或本地长期配置。

## 本次实测

- doctor、真实简洁demo与ruff通过；pytest **48 passed、1 skipped**，跳过项为Windows符号链接权限。
- 新增混音频谱测试实测同时保留源440Hz与音乐880Hz，音量比例符合配置；既有单独音乐替换测试仍通过。
- 最终视频完整解码通过，无检测到的黑帧；实际综合响度-20.33 LUFS，真峰值-5.72 dBTP。全部10条源素材散列与昨日报告一致。
- 本次查看10条素材与参考片时间序列、14段成片中点、11处叠化前中后帧，另外复核同名标题跨转场保持。未执行完整实时播放、教练逐项验收或手机真机测试。
- video-2022已到READY；一档HLS、63分片、124.933秒，首尾分片实际解码通过。私人播放ID、签名链接与上传回执只保存在交付目录。
- 专用OSS用户可访问新bucket，实测访问既有既有点播存储被拒绝；凭据写入Infisical后逐值回读一致。
- 独立OSS成片、方案、质量报告、覆盖表与音乐署名均已上传。成片全量回读SHA-256与本地一致；首次中继线路上传超时后清理分片，改用备用线路成功。

## 回退

昨日成片与工程未覆盖。新样式与混音为显式配置，旧方案继续可渲染。云资源当前配置和撤销路径已登记Mac infra的aliyun目录；删除存储数据不属于自动回退步骤。

本轮未提交、推送或合并代码，也未变更手机自助平台部署。

## 跨设备接续

用户要求bucket长期使用且不带日期，当前使用无日期的专用私有存储；凭据路径不变。代码、提示词与本次实际素材同步到另一台工作机，未依赖早先压缩版素材。该机环境检查与49项测试通过；实际以本次前两段素材渲染9.47秒，大字幕、混音、转场与完整解码通过。素材下载以逐文件SHA-256校验完成为准。

## v5：移除配乐、恢复教练原声

用户明确指出配乐影响教练讲话辨认，要求不要配乐。这是本片后续修改，替代v4的混音要求，已同步AGENTS、题材提示词、交付提示词与source-badge示例。

v5从原素材重新取同期音轨，无音乐素材和外加音轨。恢复原始音量后，增加显式可选的两遍响度处理（normalize_loudness），目标-16 LUFS和-1.5 dBTP，视频流直接复制。旧方案默认不启用，不改变旧文件。

实测：成片124.93秒，完整解码通过；处理后综合响度-16.28 LUFS、AAC成片真峰值-1.11 dBTP。处理前后视频码流散列一致。Windows ruff通过，pytest 49 passed、1 skipped（符号链接权限）。响度改善不等于逐句听辨已验收，教练原声听感仍以用户试听反馈为准。

交付实测：v5已存入专用私有存储并全量回读校验SHA-256；播放平台READY，63个HLS分片，首尾分片解码通过。Mac收到v5成片、方案和报告并逐文件SHA-256核验，完整方案校验通过，提示词与代码已同步，Mac实际50项测试通过。

## v6：每片段进出动画、序号与自主剪辑提示词

用户后续反馈要求每个片段加更活泼的出入场动画和序号，并明确长期重点是提示词与 AI 自主剪辑，经验进入 GitHub。已替代旧的无字幕动画规则，保留 v5 教练原声与无配乐。

- 本地 `git pull --ff-only` 显示远端已最新；保留本机此前同步但未提交的 v5 修改，与本次依赖的实现一并整理到独立分支，不纳入本地 IDE 文件。
- 方案新增显式 `number_clips`、`motion_style` 和逐片段 `transition_style`；旧方案默认不变。提供动画编号示例、Schema 与可复用主提示词。
- 14个片段逐段显示01—14序号；标题交替左右滑入回弹和滑出。首尾推拉限制在完整动作证据外，不调整动作速度、剪辑区间和覆盖；11处交叠剪口交替滑动／擦除，2处同源连续剪口不人为加重叠。
- v6实际成片124.93秒、720×1280、30fps，完整解码通过，未检测到黑帧；响度-16.28 LUFS、真峰值-1.27 dBTP。原素材音轨直接重建，不从旧配乐版提取。
- 查看14段标题稳定帧、开头／结尾动画8帧及全部13处剪口前中后39帧。没有执行完整实时观看、手机真机或教练专业验收。
- doctor、简洁demo、ruff和52项测试通过；新增测试实际渲染并检查编号入场、停留、出场像素，验证转场后的时长与原声音轨。
- 历史分析确认本地参考片的课程考核／成果展示结构；用户随后提供两条单作品链接，已取得标题与106秒／111秒的页面元数据，视频流仍受限。链接、观察范围和边界写入自主剪辑流程。
- v6上传尝试返回401，本机prod登录态实测失效；尚未生成可播放的新链接。需恢复登录后复用同一交付路径继续；本次未声称已上传或已通过手机验收。

长期流程与主提示词见 [自主剪辑目标](autonomous-editing.md)。本地剪辑闭环与已部署的无人值守服务分开记录。

稳定性修复：复跑时FFmpeg 9的多输入叠加进程停滞，终止该测试进程后为复杂滤镜显式限制一个工作线程；重新运行52项测试全部通过。限制与已有编码线程设置独立，避免只限制编码器而滤镜仍无限并行。
