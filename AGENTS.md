> 接续入口：[手机工作室交代文档](docs/handoff-mobile-workspace.md)。2026-09-19用户在交接后明确要求继续；先查在途任务，完成线上验收，勿重复提交。

> 2026-09-19 最新优先级：用户已恢复手机自助平台并授权腾讯云 services 部署，覆盖下文暂停说明。先读 [手机平台进度](docs/task-mobile-editing.md)。

# 开发与执行交接

> 2026-09-19 当前执行任务：[账号增长、提示词与自动剪辑](docs/task-2026-09-19-account-growth.md)。新任务已恢复执行，替代此前“今天只记录”的时间限制；分阶段更新证据与进度。

> 长期交付要求：每批素材用含日期时分秒的独立OSS目录；同批所有版本与每轮提示词调教记录都归档。见[存储位置与执行规则](docs/oss-archive.md)。点播交付不能替代剪辑OSS归档。

> 2026-09-18 晚最新纠正：先梳理教学项目；序号按项目而非片段，合并重复深水探底，序号与名称同一行；底板待定。按视频类型、剪辑风格、专业术语三层整理提示词。今天只记录，下一次按[接续清单](docs/next-session-content-review.md)执行；此前v6规则与本条冲突时以本条为准。

本仓库长期目标是手机使用、服务器执行的私人剪辑平台。当前优先级是手机上传、自动剪辑、历史工程及版本、下载。2026-09-19最小平台已部署；现有本地工具与点播交付适配器保留，完整边界见手机交代文档。

## 反馈必须进入项目

- 用户给出明确修改意见后，主动更新受影响的项目提示词、示例、流程、代码与回归检查，不必等待用户再次说“更新提示词”。只记录有依据的结论；用户尚未看过新版，不能写“已获观感认可”。
- 记录反馈日期与适用范围；新意见优先，旧规则要标明被替代。2026-09-17 的“一个一个展示、不要预告和黑底”替代了此前试行的“字幕提前预告”。
- 2026-09-18 本片反馈替代 09-17 的透明底、无转场和静音：大号动作名称加半透明底板，动作完成后采用短转场。09-18后续反馈明确不要配乐，恢复教练原声并保证可听清，替代此前背景音乐混低音量原声的要求。对照参考片文字与连续画面核对教学名称，不把俗称当标准术语，不把片段数当动作数。保留无预告、同类名称稳定的约定；单片音量与颜色不升级成账号全局偏好。
- 这是项目工作方式与模板的更新授权，不代表可以修改其他项目、账号偏好数据库、上传未经授权的素材或自动合并 PR。

- 2026-09-18 后续反馈：本片每个片段都要进出动画和序号，替代此前“无字幕动画”和仅柔和叠化。编号表示片段顺序，不代表动作种类。保留教练原声、不配乐。长期重点是自主剪辑和可复用提示词：先读 `docs/autonomous-editing.md`，主动找最新工程、继承规则、执行和记录，避免反复要求用户详述。历史作品只记录实际观察证据；无法访问时标为待分析。

## 从这里开始

1. 读 `README.md`、`docs/requirements.md`、`docs/next-steps.md`；产品边界以用户最新要求为准。
2. 检查 `git status`，保护已有修改。Python 用 `uv`，Node 如需使用则用 `pnpm`。遵守仓库锁文件和共享缓存，不复制依赖树。
3. 执行 `uv sync --locked`、`uv run editing doctor`、`uv run editing demo --simple`。
4. 执行 `uv run ruff check --no-cache .`、`uv run pytest`。测试生成的素材放系统临时目录，结束自动清理。
5. 从 `docs/next-steps.md` 选择当前可做的工作，逐项更新证据；不需要知道前任的对话 ID。

## 代码地图

- `src/video_editing/contracts.py`：唯一方案类型来源；`schemas/edit-plan.schema.json` 是其导出物。
- `validation.py`：真实时长、来源角色、独有内容覆盖、连续画面证据、转场和音轨检查。
- `engine.py`：固定提交的 vedit 适配器、中文字幕、FFmpeg 兼容、校验后交付。
- `media.py`：只读探测、受限素材路径、环境检查。
- `demo.py`：可重复生成的合成素材，不含私人视频。
- `delivery.py`：复用 video-cli 登录与接口的本地上传、重试回执及 HLS 检查，不是后台平台。
- `docs/agent-handoff.md`：剪辑 agent 与编码 agent 的输入输出、错误处理和验收。

## 必须保留的约束

- 用户只有现有编程助手订阅，没有另购模型 API 凭证。渲染器不调用模型；服务端执行器以后通过独立适配接口接入。
- 不把 Mac、GUI 自动化或某个执行者的名字写进产品依赖；代码、路径、提交及 PR 使用任务名称。仓库与 PR 默认用中文。
- `video-2022` 负责视频文件，剪辑平台负责工程、规则、版本和任务。不得擅自改点播仓库或将素材公开。
- 参考样片默认只用于理解。没有本次明确依据，不得拿参考片的镜头补齐原素材缺项。
- 原声是证据之一，不是动作发生时间的真值。必须看连续画面；泳姿、出发、转身和其他技能分开分类。
- 不自动压缩、加速或漏掉必需内容以凑时长。无法辨认、时长冲突和素材不足必须显式报告。
- 明确反馈同步项目模板；单次音乐、时长等选择只进本次规则快照，不能擅自升级成账号全局偏好。历史工程补新字段时保留其原先样式。
- 不改动原素材、不覆盖以前的成片。原片、音轨、凭证、报告中的私人数据均不得提交 Git。

## 运行与临时文件

只执行结构化参数，禁止把自然语言或模型返回文本拼进 shell。素材通过受限根目录映射，本地 CLI 不接受任意网络 URL。

一次性工作使用 `tempfile.TemporaryDirectory`，或读取系统 `TMPDIR` 后 `mktemp` 并预先注册 `trap`。清理只针对验证过的本次临时目录。可重试所需的方案和工程版本属于持久数据，不是缓存。

引擎运行采用一任务一进程；`VEDIT_CACHE` 在进程内指向任务临时目录。不要同时在线程中调用渲染适配器，不要运行上游的自动安装、自动注册或全局配置脚本。

改完代码用真实合成素材验证结果；改工具链或锁定版本时重新跑相关集成测试。编译成功、完整解码成功、画面审核、手机保存、部署成功应分别报告。

用户说提交 PR 或推送，只授权提交/推送/创建 PR，不等于授权合并。合并状态、测试通过与产品完成是三个不同结论。交接要列清已实现、已跑通、未确认和未实现；没有这些边界的“完成”不算合格汇报。
